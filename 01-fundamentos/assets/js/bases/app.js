//console.log('Hello world!');

/**
 * Variables:
 *      * var: variable global
 *          - se puede reasignar.
 *          - No se recomienda usar var.
 *      * let: variable local
 *          - se puede reasignar.
 *      * const: constante
 *          - no se puede reasignar.
 */
let a = 10,
    b = 20,
    c = 'Hola'
    d = 'Batman';
    x = a + b;

const saludo = `${c} ${d}`;
/**
 * Logs:
 *      * console.log: imprime log en la consola.
 *      * console.error: imprime error en la consola.
 *      * console.warn: imprime warning en la consola.
 * Para imprimir nombre de variable y valor:
 *     * console.log({variable});
 */

c = 'Hola de nuevo'

//alert(saludo);
